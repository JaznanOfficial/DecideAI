---
trigger: always_on
---

always use bun, never use anything else.
we'll use neondb, drizzle, better auth, postgresql, zod, shadcn, next.js, tanstack query for data fetching, react hook form, sonner(rich text),better-auth etc.
always follow same pattern for same kind of functionalities or flow.